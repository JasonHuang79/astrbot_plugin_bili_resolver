# 更新日志

## v1.0.6

- 修复：跨机部署（astrbot 与 NapCat 不在同一主机）时合并转发失败
  - 原因：渲染卡片是 astrbot 本地临时文件，远程 OneBot 客户端 `stat` 该路径报 EACCES/ENOENT，`send_group_forward_msg` 被拒后整条回退普通回复
  - 修复：合并转发节点内的渲染图改为 **base64 内联**（`file=base64://...`），客户端可独立解码，不再依赖共享文件系统
  - 内联上限 1.5MB，超出自动回退文本节点

## v1.0.5

- 新增 HTML 卡片渲染：解析结果可通过 AstrBot `html_render`（t2i）服务渲染成精美卡片图片，作为合并转发「结果节点」的内容发送
  - 内置三套模板：`template_1` 经典 / `template_2` B站粉（默认）/ `simple` 简约，运行期可用 `/卡片样式` 指令切换
  - 配置项 `enable_render`（默认开启）与 `renderer_template`；渲染失败或未配置渲染服务时自动回退文本节点
  - 需在 AstrBot 侧配置可用的 html_render / t2i 服务（如公共接口或自部署镜像）

## v1.0.4

- 解析结果改为以 QQ 合并转发卡片发送（配置项 `enable_forward`，默认开启）
  - 自动解析：卡片含「发送者原文 + 解析结果」两个节点
  - `/搜视频`：卡片含解析结果节点
  - 节点内包含文字摘要与封面图；客户端不支持合并转发时自动退回普通回复

## v1.0.3

- 新增视频解析排版风格选择：原始格式 / 简洁风格 / 自定义
- 自定义模式支持变量：`${标题}` `${UP主}` `${UP主链接}` `${简介}` `${封面}` `${点赞}` `${投币}` `${收藏}` `${转发}` `${观看}` `${弹幕数量}` `${评论}` `${链接}` `${发布时间}` `${类型}` `${BV号}` `${时长}` `${版权}`

**原始格式**（默认）：

```
https://www.bilibili.com/video/BVxxxxxxxxxx

标题："视频标题"
类型：分区 | UP：UP主名称 | https://space.bilibili.com/xxx

播放：xxx万 | 弹幕：xxx | 收藏：xxx
点赞：xxx | 硬币：xxx | 评论：xxx

简介：-
```

**简洁风格**：

```
🎬 标题：视频标题
👤 UP主：UP主名称
📝 简介：-
[封面图]
👍 点赞：xxx 🪙 投币：xxx
❤️ 收藏：xxx 🔄 转发：xxx
👀 观看：xxx 💬 弹幕：xxx
```

## v1.0.2

- 更新插件介绍
- 修复多段文字被拆成多条消息发送的问题
